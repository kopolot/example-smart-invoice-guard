# CI / nektos-act overrides.
services:
    php:
        ports: !override []
        # GHA runner UID ≠ image user `container` (1000). Without this, composer/npm
        # cannot write composer.lock, vendor/, node_modules/ on the bind mount.
        user: "0:0"
    nginx:
        ports: !override []
