; Local PHP-FPM pool overrides.
; Mounted at /usr/local/etc/php-fpm.d/zz-app.conf — edit and restart the php service.
[www]
user = container
group = container
listen = 9000
clear_env = no
catch_workers_output = yes
decorate_workers_output = no

pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6
pm.max_requests = 500
pm.status_path = /status

request_terminate_timeout = 300
